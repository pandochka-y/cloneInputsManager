document.addEventListener('DOMContentLoaded', () => {
    const test1 = new managerInputs('.calcMon',{
        classRender: '.node-copy',
        classListRender: '.node-list',
        initItems: 2,
        maxItems: 12,
        classButtonAdd: '.add-button',
        classButtonDelete: '.delete',
    })
})